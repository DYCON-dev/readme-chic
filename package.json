{
  "name": "readme-chic",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo for the readme-chic Cloudflare Worker (SVG renderer) and Vue UI",
  "scripts": {
    "deploy-worker": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
